class Item {
  constructor(id, name, des, categoryid , url, price, sugar, ice) {
    this.itemId = id
    this.itemName = name
    this.itemDes = des
    this.categoryId=categoryid
    this.itemUrl = url
    this.itemPrice= price
    this.itemNoteSugar = sugar
    this.itemNoteIce = ice
  }
}
export default Item;
